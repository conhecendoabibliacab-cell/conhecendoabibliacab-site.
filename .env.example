# Copie este arquivo para .env (não commitar o .env)

# Key da IA (Gemini - Google Generative Language API)
# Usada em: netlify/functions/generate-content.js e scripts/generate_daily.js
GOOGLE_API_KEY=

# API da Bíblia (opcional). Por padrão usamos https://bible-api.com (sem chave).
# Se você tiver outro provedor que exige chave, configure abaixo.

# URL base do provedor da Bíblia (deixe padrão para bible-api.com)
BIBLE_API_BASE_URL=https://bible-api.com

# Tradução a usar (para bible-api.com)
BIBLE_TRANSLATION=almeida

# Chave da API da Bíblia (opcional). Se seu provedor exigir.
BIBLE_API_KEY=

# Nome do header da chave (opcional). Ex.: API-KEY, X-API-Key, Authorization
# Se vazio e BIBLE_API_KEY existir, será usado Authorization: Bearer <key>
BIBLE_API_HOST_HEADER=